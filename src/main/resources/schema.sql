CREATE TABLE users (
  id       INTEGER PRIMARY KEY,
  login    VARCHAR(30),
  password VARCHAR(30),
  email    VARCHAR(30)
);
--
-- CREATE TABLE books (
--   id       INTEGER PRIMARY KEY,
--   title    VARCHAR(30),
-- );